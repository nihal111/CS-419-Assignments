For this part 1 I have used the following normalisation-

 X_i = (X_i - X_min)/X_ptp

 where-
 X_min is the minimum value of all X_i
 X_ptp is the range (max - min) of all X_i

 This normalisation has been applied to all input features and even the output (price).
 This results in a range of [0,1]